---
import Icon from "./Icon.astro";
---

<section class="grid grid-cols-1 md:grid-cols-12 gap-4 mb-4" id="hero">
  <!-- Main Title Cell -->
  <div
    class="col-span-1 md:col-span-8 lg:col-span-9 rounded-softer p-6 relative overflow-hidden group "
  >
    <div class="h-full flex flex-col justify-center gap-8 relative z-10">
      <div>
        <h1
          class="text-6xl md:text-7xl lg:text-8xl font-black tracking-tighter-soft text-white mb-2 leading-[1.05]"
        >
          Maksym<br />Baranov
        </h1>
      </div>
      <p
        class="text-md md:text-xl  max-w-2xl font-normal leading-relaxed-soft tracking-normal-soft"
      >
        As a <span class="text-primary font-semibold">Product Engineer</span>, I
        care as much about the human experience as the underlying logic. I
        leverage AI to move at speed, but I rely on empathy and engineering
        craft to create tools that people actually enjoy using.
      </p>
    </div>
    <!-- Animated gradient background -->
    <div class="absolute inset-0 pointer-events-none hero-gradient-overlay">
    </div>
    <div class="absolute inset-0 pointer-events-none hero-animated-gradient">
    </div>
  </div>

  <!-- AI Stack Widget Cell -->
  <div
    class="col-span-1 md:col-span-4 lg:col-span-3 bento-card rounded-softer p-6 flex flex-col justify-between group glow-hover relative overflow-hidden"
  >
    <div class="absolute inset-0"></div>
    <div class="relative z-10">
      <div class="flex items-center justify-between mb-4">
        <span
          class="text-xs font-mono text-primary uppercase tracking-wide-soft border border-primary px-3 py-1.5 rounded-full"
          >The Workflow</span
        >
        <Icon name="bolt" size={24} class="text-primary" />
      </div>
      <div class="flex flex-col gap-4 my-4">
        <div
          class="workflow-item workflow-item-1 flex items-center gap-3 text-sm font-mono leading-relaxed"
        >
          <Icon
            name="architecture"
            size={16}
            class="text-base workflow-icon-1"
          />
          <span class="workflow-text-1">Strategy</span>
        </div>
        <div
          class="workflow-line workflow-line-1 w-0.5 h-4 bg-white/50 rounded-full mx-2"
        >
        </div>
        <div
          class="workflow-item workflow-item-2 flex items-center gap-3 text-sm font-mono leading-relaxed"
        >
          <Icon name="smart_toy" size={16} class="text-base workflow-icon-2" />
          <span class="workflow-text-2">AI</span>
        </div>
        <div
          class="workflow-line workflow-line-2 w-0.5 h-4 bg-white/50 rounded-full mx-2"
        >
        </div>
        <div
          class="workflow-item workflow-item-3 flex items-center gap-3 text-sm font-mono leading-relaxed"
        >
          <Icon name="code" size={16} class="text-base workflow-icon-3" />
          <span class="workflow-text-3">Adjustment</span>
        </div>
        <div
          class="workflow-line workflow-line-3 w-0.5 h-4 bg-white/50 rounded-full mx-2"
        >
        </div>
        <div
          class="workflow-item workflow-item-4 flex items-center gap-3 text-sm font-mono leading-relaxed"
        >
          <Icon name="done" size={16} class="text-base workflow-icon-4" />
          <span class="workflow-text-4">Completed product</span>
        </div>
      </div>
    </div>
    <div class="relative z-10 mt-4 pt-4 border-t border-surface-border-soft">
      <p class="text-xs text-text-dim font-mono mb-1.5 tracking-wide-soft">
        Time to Deploy
      </p>
      <p class="text-2xl font-semibold text-white leading-snug">
        &lt; 24 Hours
      </p>
    </div>
  </div>
</section>

<style>
  .hero-gradient-overlay {
    background:
      radial-gradient(
        circle at 0% 100%,
        rgba(91, 141, 239, 0.12) 0%,
        transparent 60%
      ),
      radial-gradient(
        circle at 10% 90%,
        rgba(91, 141, 239, 0.08) 0%,
        transparent 70%
      ),
      linear-gradient(45deg, rgba(91, 141, 239, 0.06) 0%, transparent 50%);
    opacity: 1;
    transition: opacity 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .hero-gradient-overlay:hover {
    opacity: 1.1;
  }

  .hero-animated-gradient {
    background: linear-gradient(
      to top,
      rgba(91, 141, 239, 0.18) 0%,
      transparent 70%
    );
    animation: shimmer 5s ease-in-out infinite;
    bottom: 0;
    top: auto;
    height: 100%;
  }

  @keyframes shimmer {
    0%,
    100% {
      opacity: 0.5;
    }
    50% {
      opacity: 0.9;
    }
  }

  .hero-animated-gradient::before {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 100%;
    background:
      radial-gradient(
        circle at 20% 80%,
        rgba(91, 141, 239, 0.25) 0%,
        transparent 50%
      ),
      radial-gradient(
        circle at 80% 90%,
        rgba(91, 141, 239, 0.18) 0%,
        transparent 60%
      );
    animation: pulse 4s ease-in-out infinite;
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 0.3;
    }
    50% {
      opacity: 0.7;
    }
  }

  /* Workflow lift animation */
  .workflow-text-1,
  .workflow-text-2,
  .workflow-text-3,
  .workflow-text-4 {
    color: #b5b5b5;
    transition: color 0.5s ease-in-out;
  }

  .workflow-icon-1,
  .workflow-icon-2,
  .workflow-icon-3,
  .workflow-icon-4 {
    color: currentColor;
    transition: color 0.6s ease-in-out;
  }

  .workflow-item-1 {
    animation: workflowLift 8s ease-in-out infinite;
  }

  .workflow-item-2 {
    animation: workflowLift 8s ease-in-out infinite;
    animation-delay: 2s;
  }

  .workflow-item-3 {
    animation: workflowLift 8s ease-in-out infinite;
    animation-delay: 4s;
  }

  .workflow-item-4 {
    animation: workflowLift 8s ease-in-out infinite;
    animation-delay: 6s;
  }

  @keyframes workflowLift {
    0%,
    20% {
      color: #b5b5b5;
    }
    5%,
    15% {
      color: #5b8def;
    }
    25%,
    100% {
      color: #b5b5b5;
    }
  }

  .workflow-item-1 .workflow-text-1,
  .workflow-item-1 .workflow-icon-1 {
    animation: workflowLiftText 8s ease-in-out infinite;
  }

  .workflow-item-2 .workflow-text-2,
  .workflow-item-2 .workflow-icon-2 {
    animation: workflowLiftText 8s ease-in-out infinite;
    animation-delay: 2s;
  }

  .workflow-item-3 .workflow-text-3,
  .workflow-item-3 .workflow-icon-3 {
    animation: workflowLiftText 8s ease-in-out infinite;
    animation-delay: 4s;
  }

  .workflow-item-4 .workflow-text-4,
  .workflow-item-4 .workflow-icon-4 {
    animation: workflowLiftText 8s ease-in-out infinite;
    animation-delay: 6s;
  }

  @keyframes workflowLiftText {
    0%,
    20% {
      color: #b5b5b5;
    }
    5%,
    15% {
      color: #5b8def;
    }
    25%,
    100% {
      color: #b5b5b5;
    }
  }
</style>
